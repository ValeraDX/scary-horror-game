name: "godot-ci export"

on:
  push:

env:
  GODOT_VERSION: 4.7.2
  PROJECT_PATH: .

jobs:
  export-windows-32:
    name: Windows 32 Bit Export
    runs-on: ubuntu-24.04

    container:
      image: barichello/godot-ci:4.7.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

      - name: Install Blender
        run: |
          apt-get update
          apt-get install -y --no-install-recommends blender
          blender --background --version
          test -x /usr/bin/blender

      - name: Import project
        run: |
          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --import \
            --audio-driver Dummy \
            --verbose

      - name: Windows 32 Bit Build
        run: |
          mkdir -p build/win32

          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --verbose \
            --audio-driver Dummy \
            --export-release \
            "Windows 32 Bit" \
            "build/win32/scary horror-win32.exe"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-32-bit
          path: build/win32

  export-windows-64:
    name: Windows 64 Bit Export
    runs-on: ubuntu-24.04

    container:
      image: barichello/godot-ci:4.7.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

      - name: Install Blender
        run: |
          apt-get update
          apt-get install -y --no-install-recommends blender
          blender --background --version
          test -x /usr/bin/blender

      - name: Import project
        run: |
          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --import \
            --audio-driver Dummy \
            --verbose

      - name: Windows 64 Bit Build
        run: |
          mkdir -p build/win64

          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --verbose \
            --audio-driver Dummy \
            --export-release \
            "Windows 64 Bit" \
            "build/win64/scary horror-win64.exe"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-64-bit
          path: build/win64

  export-linux-32:
    name: Linux 32 Bit Export
    runs-on: ubuntu-24.04

    container:
      image: barichello/godot-ci:4.7.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

      - name: Install Blender
        run: |
          apt-get update
          apt-get install -y --no-install-recommends blender
          blender --background --version
          test -x /usr/bin/blender

      - name: Import project
        run: |
          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --import \
            --audio-driver Dummy \
            --verbose

      - name: Linux 32 Bit Build
        run: |
          mkdir -p build/linux32

          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --audio-driver Dummy \
            --verbose \
            --export-release \
            "Linux 32 Bit" \
            "build/linux32/super scary-linux32"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-32-bit
          path: build/linux32

  export-linux-64:
    name: Linux 64 Bit Export
    runs-on: ubuntu-24.04

    container:
      image: barichello/godot-ci:4.7.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

      - name: Install Blender
        run: |
          apt-get update
          apt-get install -y --no-install-recommends blender
          blender --background --version
          test -x /usr/bin/blender

      - name: Import project
        run: |
          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --import \
            --audio-driver Dummy \
            --verbose

      - name: Linux 64 Bit Build
        run: |
          mkdir -p build/linux64

          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --verbose \
            --audio-driver Dummy \
            --export-release \
            "Linux 64 Bit" \
            "build/linux64/scary horror-linux"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-64-bit
          path: build/linux64

  export-mac:
    name: macOS Export
    runs-on: ubuntu-24.04

    container:
      image: barichello/godot-ci:4.7.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

      - name: Install Blender
        run: |
          apt-get update
          apt-get install -y --no-install-recommends blender
          blender --background --version
          test -x /usr/bin/blender

      - name: Import project
        run: |
          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --import \
            --audio-driver Dummy \
            --verbose

      - name: macOS Build
        run: |
          mkdir -p build/mac

          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --verbose \
            --audio-driver Dummy \
            --export-release \
            "macOS" \
            "build/mac/scary horror-mac.zip"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos
          path: build/mac

  export-android:
    name: Android Debug Export
    runs-on: ubuntu-24.04

    container:
      image: barichello/godot-ci:4.7.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        run: |
          mkdir -p "$HOME/.local/share/godot/export_templates"
          cp -a "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" "$HOME/.local/share/godot/export_templates/"

          mkdir -p "$HOME/.config/godot"
          cp /root/.config/godot/editor_settings-4.7.tres "$HOME/.config/godot/editor_settings-4.7.tres"

      - name: Verify Android SDK
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          java -version
          sdkmanager --version
          test -d "$ANDROID_HOME"
          test -d "$ANDROID_HOME/platform-tools"
          test -d "$ANDROID_HOME/build-tools/33.0.2"
          test -d "$ANDROID_HOME/platforms/android-33"
          test -d "$ANDROID_HOME/ndk"

      - name: Install Blender
        run: |
          apt-get update
          apt-get install -y --no-install-recommends blender
          blender --background --version
          test -x /usr/bin/blender

      - name: Import project
        run: |
          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --import \
            --audio-driver Dummy \
            --verbose

      - name: Verify project assets
        run: |
          find "$PROJECT_PATH" \
            -type f \
            \( -iname "*.wav" -o -iname "*.ogg" -o -iname "*.mp3" \) \
            -print

          find "$PROJECT_PATH" \
            -type f \
            \( -iname "*.png" \
            -o -iname "*.jpg" \
            -o -iname "*.jpeg" \
            -o -iname "*.webp" \
            -o -iname "*.svg" \) \
            -print

      - name: Android Debug Build
        run: |
          mkdir -p build/android

          godot \
            --headless \
            --path "$PROJECT_PATH" \
            --verbose \
            --audio-driver Dummy \
            --export-debug \
            "Android" \
            "build/android/scary horror-android.apk"

      - name: Verify APK
        run: |
          test -f "build/android/scary horror-android.apk"
          ls -lh build/android

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-debug
          path: build/android
